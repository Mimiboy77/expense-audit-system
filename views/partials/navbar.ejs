<nav class="navbar">
  <div class="navbar-brand">
    <a href="/dashboard">ExpenseAudit</a>
  </div>

  <ul class="navbar-links">
    <li><a href="/expenses">My Expenses</a></li>
    <li><a href="/expenses/history">History</a></li>

    <% if (user.role === "manager" || user.role === "finance") { %>
      <li><a href="/approvals">Approvals</a></li>
    <% } %>

    <% if (user.role === "finance") { %>
      <li><a href="/audit-logs">Audit Logs</a></li>
      <li><a href="/reports">Reports</a></li>
      <li><a href="/admin">Admin</a></li>
    <% } %>

    <% if (user.role === "manager" || user.role === "finance") { %>
      <li><a href="/budget">Budget</a></li>
    <% } %>
  </ul>

  <div class="navbar-user">
    <!-- Notification bell with unread count badge -->
    <a href="/notifications" class="notification-bell">
      🔔
      <% if (user.unreadCount > 0) { %>
        <span class="notification-badge">
          <%= user.unreadCount %>
        </span>
      <% } %>
    </a>

    <span><%= user.name %> (<%= user.role %>)</span>
    <a href="/profile" class="btn-small">My Profile</a>
    <a href="/logout" class="btn-logout">Logout</a>
  </div>
</nav>